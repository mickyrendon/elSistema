// getting inputs value from card html
const date = document.querySelector('.date').value
const ammount = document.querySelector('.ammount').value
const name = document.querySelector('.name').value
const indicator = document.querySelector('.indicator').value
const fee = document.querySelector('.fee').value
const days = document.querySelector('.days').value

const cardData = []

cardData.forEach(value => {
    cardData.push(value)
})

// guardar el valor de las constantes en un array para poder exportarlo al index.js y mostrarlo por pantalla